{
    "private": true,
    "workspaces": ["Server","battleship","shared"],
    "scripts": {
        "preinstall": "yarn install",
        "start": "npm --prefix battleship run build && node Server/server.js"
    }
}